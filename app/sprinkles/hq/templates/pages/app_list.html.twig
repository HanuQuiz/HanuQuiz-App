{% extends "pages/abstract/dashboard.html.twig" %}

{% block stylesheets_page %}
    <!-- Page-specific CSS asset bundle -->
    {{ assets.css('css/form-widgets') | raw }}
{% endblock %}

{# Overrides blocks in head of base template #}
{% block page_title %}{{ translate("APP", 2)}}{% endblock %}

{% block page_description %}{{ translate("APP.PAGE_DESCRIPTION")}}{% endblock %}

{% block body_matter %}
    <div class="row">
        <div class="col-md-12">
            <div id="widget-apps" class="box box-primary">
                <div class="box-header">
                    <h3 class="box-title pull-left"><i class="fa fa-fw fa-user"></i> {{translate('APP', 2)}}</h3>
                    {% include "tables/table-tool-menu.html.twig" %}
                </div>
                <div class="box-body">
                    {% include "tables/app_list.html.twig" with {
                            "table" : {
                                "id" : "table-apps"
                            }
                        }
                    %}
                </div>
                {% if checkAccess('create_app') %}
                <div class="box-footer">
                    <button type="button" class="btn btn-success js-app-create">
                        <i class="fa fa-plus-square"></i>  {{ translate("APP.CREATE")}}
                    </button>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts_page %}
    <!-- Include validation rules -->
    <script>
    {% include "pages/partials/page.js.twig" %}
    </script>

    <!-- Include form widgets JS -->
    {{ assets.js('js/form-widgets') | raw }}

    <!-- Include page-specific JS -->
    {{ assets.js('js/pages/apps') | raw }}

{% endblock %}